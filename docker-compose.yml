version: "3.9"

services:
  vartapravah:
    build: .
    container_name: vartapravah_ai
    
    # Environment variables
    environment:
      - PYTHONUNBUFFERED=1
      - TTS_MODEL=tts_models/multilingual/multi-dataset/xtts_v2
      - YOUTUBE_RTMP_KEY=qcu7-xesd-m4sv-9zvv-e335
    
    # Volume mounts
    volumes:
      - ./output:/app/output
      - ./assets:/app/assets
      - ./logs:/app/logs
    
    # Port mapping
    ports:
      - "8000:8000"
    
    # Health check
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
    # Restart policy
    restart: always
    
    # Resource limits
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 2G

